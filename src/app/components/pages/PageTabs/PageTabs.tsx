"use client"
import React from 'react';
import {observer} from "mobx-react-lite";

const PageTabs = () => {
    return (
        <div>
            Страница "Иное" в разработке
        </div>
    );
};

export default observer(PageTabs);