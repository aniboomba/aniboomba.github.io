"use client"
import React from 'react';
import {observer} from "mobx-react-lite";

const PageProfile = () => {
    return (
        <div>
            Страница "Профиль" в разработке
        </div>
    );
};

export default observer(PageProfile);